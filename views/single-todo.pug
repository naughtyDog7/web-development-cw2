extends layout.pug

block content
  main(class="container w-25 m-3")
    h2= todo.title
    p= todo.value 
    form(action="/todos", method="get") 
      button(class="btn btn-primary mt-1" type="submit") Back